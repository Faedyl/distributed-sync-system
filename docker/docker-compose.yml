# AUTO-GENERATED by tools/gen_compose.py — do not edit by hand.
# Regenerate with:  python tools/gen_compose.py --lock N --queue M --cache K
x-node-base: &node-base
  build:
    context: ..
    dockerfile: docker/Dockerfile.node
  networks: [dsync]
  restart: unless-stopped
  env_file: ../.env.example

services:
  redis:
    image: redis:7-alpine
    networks: [dsync]
    profiles: [lock, queue, cache, all]
    restart: unless-stopped

  # ---------- lock cluster (5 nodes) ----------
  lock1:
    <<: *node-base
    profiles: [lock, all]
    container_name: lock1
    environment:
      COMPONENT: lock_manager
      NODE_ID: lock1
      NODE_BIND: 0.0.0.0:8001
      NODE_PORT: "8001"
      PEERS: "lock1@lock1:8001,lock2@lock2:8001,lock3@lock3:8001,lock4@lock4:8001,lock5@lock5:8001"
      DATA_DIR: /var/lib/dsync
      LOG_LEVEL: INFO
    ports: ["18011:8001"]
    volumes: ["lock1-data:/var/lib/dsync"]

  lock2:
    <<: *node-base
    profiles: [lock, all]
    container_name: lock2
    environment:
      COMPONENT: lock_manager
      NODE_ID: lock2
      NODE_BIND: 0.0.0.0:8001
      NODE_PORT: "8001"
      PEERS: "lock1@lock1:8001,lock2@lock2:8001,lock3@lock3:8001,lock4@lock4:8001,lock5@lock5:8001"
      DATA_DIR: /var/lib/dsync
      LOG_LEVEL: INFO
    ports: ["18012:8001"]
    volumes: ["lock2-data:/var/lib/dsync"]

  lock3:
    <<: *node-base
    profiles: [lock, all]
    container_name: lock3
    environment:
      COMPONENT: lock_manager
      NODE_ID: lock3
      NODE_BIND: 0.0.0.0:8001
      NODE_PORT: "8001"
      PEERS: "lock1@lock1:8001,lock2@lock2:8001,lock3@lock3:8001,lock4@lock4:8001,lock5@lock5:8001"
      DATA_DIR: /var/lib/dsync
      LOG_LEVEL: INFO
    ports: ["18013:8001"]
    volumes: ["lock3-data:/var/lib/dsync"]

  lock4:
    <<: *node-base
    profiles: [lock, all]
    container_name: lock4
    environment:
      COMPONENT: lock_manager
      NODE_ID: lock4
      NODE_BIND: 0.0.0.0:8001
      NODE_PORT: "8001"
      PEERS: "lock1@lock1:8001,lock2@lock2:8001,lock3@lock3:8001,lock4@lock4:8001,lock5@lock5:8001"
      DATA_DIR: /var/lib/dsync
      LOG_LEVEL: INFO
    ports: ["18014:8001"]
    volumes: ["lock4-data:/var/lib/dsync"]

  lock5:
    <<: *node-base
    profiles: [lock, all]
    container_name: lock5
    environment:
      COMPONENT: lock_manager
      NODE_ID: lock5
      NODE_BIND: 0.0.0.0:8001
      NODE_PORT: "8001"
      PEERS: "lock1@lock1:8001,lock2@lock2:8001,lock3@lock3:8001,lock4@lock4:8001,lock5@lock5:8001"
      DATA_DIR: /var/lib/dsync
      LOG_LEVEL: INFO
    ports: ["18015:8001"]
    volumes: ["lock5-data:/var/lib/dsync"]

  # ---------- queue cluster (4 nodes) ----------
  queue1:
    <<: *node-base
    profiles: [queue, all]
    container_name: queue1
    environment:
      COMPONENT: queue_node
      NODE_ID: queue1
      NODE_BIND: 0.0.0.0:8001
      NODE_PORT: "8001"
      PEERS: "queue1@queue1:8001,queue2@queue2:8001,queue3@queue3:8001,queue4@queue4:8001"
      DATA_DIR: /var/lib/dsync
      LOG_LEVEL: INFO
      QUEUE_REPLICATION_FACTOR: "2"
      QUEUE_VIRTUAL_NODES: "128"
    ports: ["18021:8001"]
    volumes: ["queue1-data:/var/lib/dsync"]

  queue2:
    <<: *node-base
    profiles: [queue, all]
    container_name: queue2
    environment:
      COMPONENT: queue_node
      NODE_ID: queue2
      NODE_BIND: 0.0.0.0:8001
      NODE_PORT: "8001"
      PEERS: "queue1@queue1:8001,queue2@queue2:8001,queue3@queue3:8001,queue4@queue4:8001"
      DATA_DIR: /var/lib/dsync
      LOG_LEVEL: INFO
      QUEUE_REPLICATION_FACTOR: "2"
      QUEUE_VIRTUAL_NODES: "128"
    ports: ["18022:8001"]
    volumes: ["queue2-data:/var/lib/dsync"]

  queue3:
    <<: *node-base
    profiles: [queue, all]
    container_name: queue3
    environment:
      COMPONENT: queue_node
      NODE_ID: queue3
      NODE_BIND: 0.0.0.0:8001
      NODE_PORT: "8001"
      PEERS: "queue1@queue1:8001,queue2@queue2:8001,queue3@queue3:8001,queue4@queue4:8001"
      DATA_DIR: /var/lib/dsync
      LOG_LEVEL: INFO
      QUEUE_REPLICATION_FACTOR: "2"
      QUEUE_VIRTUAL_NODES: "128"
    ports: ["18023:8001"]
    volumes: ["queue3-data:/var/lib/dsync"]

  queue4:
    <<: *node-base
    profiles: [queue, all]
    container_name: queue4
    environment:
      COMPONENT: queue_node
      NODE_ID: queue4
      NODE_BIND: 0.0.0.0:8001
      NODE_PORT: "8001"
      PEERS: "queue1@queue1:8001,queue2@queue2:8001,queue3@queue3:8001,queue4@queue4:8001"
      DATA_DIR: /var/lib/dsync
      LOG_LEVEL: INFO
      QUEUE_REPLICATION_FACTOR: "2"
      QUEUE_VIRTUAL_NODES: "128"
    ports: ["18024:8001"]
    volumes: ["queue4-data:/var/lib/dsync"]

  # ---------- cache cluster (3 nodes) ----------
  cache1:
    <<: *node-base
    profiles: [cache, all]
    container_name: cache1
    environment:
      COMPONENT: cache_node
      NODE_ID: cache1
      NODE_BIND: 0.0.0.0:8001
      NODE_PORT: "8001"
      PEERS: "cache1@cache1:8001,cache2@cache2:8001,cache3@cache3:8001"
      DATA_DIR: /var/lib/dsync
      LOG_LEVEL: INFO
      CACHE_CAPACITY: "1024"
      CACHE_POLICY: "LRU"
    ports: ["18031:8001"]
    volumes: ["cache1-data:/var/lib/dsync"]

  cache2:
    <<: *node-base
    profiles: [cache, all]
    container_name: cache2
    environment:
      COMPONENT: cache_node
      NODE_ID: cache2
      NODE_BIND: 0.0.0.0:8001
      NODE_PORT: "8001"
      PEERS: "cache1@cache1:8001,cache2@cache2:8001,cache3@cache3:8001"
      DATA_DIR: /var/lib/dsync
      LOG_LEVEL: INFO
      CACHE_CAPACITY: "1024"
      CACHE_POLICY: "LRU"
    ports: ["18032:8001"]
    volumes: ["cache2-data:/var/lib/dsync"]

  cache3:
    <<: *node-base
    profiles: [cache, all]
    container_name: cache3
    environment:
      COMPONENT: cache_node
      NODE_ID: cache3
      NODE_BIND: 0.0.0.0:8001
      NODE_PORT: "8001"
      PEERS: "cache1@cache1:8001,cache2@cache2:8001,cache3@cache3:8001"
      DATA_DIR: /var/lib/dsync
      LOG_LEVEL: INFO
      CACHE_CAPACITY: "1024"
      CACHE_POLICY: "LRU"
    ports: ["18033:8001"]
    volumes: ["cache3-data:/var/lib/dsync"]

networks:
  dsync:
    driver: bridge

volumes:
  lock1-data: {}
  lock2-data: {}
  lock3-data: {}
  lock4-data: {}
  lock5-data: {}
  queue1-data: {}
  queue2-data: {}
  queue3-data: {}
  queue4-data: {}
  cache1-data: {}
  cache2-data: {}
  cache3-data: {}
