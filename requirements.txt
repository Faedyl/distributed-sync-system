# Runtime dependencies for the three node services
aiohttp>=3.10
structlog>=24.0

# Used by benchmarks/load_test_scenarios.py and demo CLI clients
httpx>=0.27

# Test runner
pytest>=8.0
pytest-asyncio>=0.24

# Optional: Locust for /benchmarks. Install separately if you want it:
#   pip install locust
